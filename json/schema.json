{
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "minLength": 2,
        "title": "Name",
        "description": "Name or alias"
      },
      "status": {
        "type": "string",
        "title": "Status",
        "enum": ["ACTIVE", "ARCHIVED", "PAUSED"]
      },
      "type": {
        "type": "string",
        "title": "Ad Type",
        "enum": ["link", "circular", "image", "auto"]
      },
      "bid": {
        "type": "number",
        "title": "Bid (in cents)"
      },
      "body": {
        "type": "string",
        "title": "Ad Body Text"
      },
      "image": {
        "type": "string",
        "title": "Image URL"
      }
    }
}
